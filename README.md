# 🐱 CATS (Container Allocation & Tracking System)

**CATS** - це система для автоматизації обліку, розподілу та відстеження промислової тари. Проєкт розроблений з використанням сучасних підходів до архітектури програмного забезпечення.

## 🚀 Технологічний стек

* **Платформа:** .NET 9 (C#)
* **Архітектура:** Clean Architecture (Domain, Application, Infrastructure, API)
* **База даних:** PostgreSQL 15 (Alpine)
* **ORM:** Entity Framework Core
* **Контейнеризація:** Docker & Docker Compose
* **Документація:** Swagger UI / OpenAPI

## 🛠 Початок роботи

### Передумови
Для запуску вам знадобиться:
* [Docker Desktop](https://www.docker.com/products/docker-desktop)
* [.NET 9 SDK](https://dotnet.microsoft.com/download/dotnet/9.0) (для локальної розробки)
* IDE: JetBrains Rider або Visual Studio

---

### 🐳 Спосіб 1: Запуск через Docker (Full Stack)

Цей спосіб піднімає і базу даних, і API в ізольованих контейнерах. Ідеально для демонстрації роботи.

1.  Клонуйте репозиторій:
    ```bash
    git clone <https://github.com/Hyptofon/CATS.git>
    cd CATS
    ```
2.  Запустіть проєкт однією командою:
    ```bash
    docker-compose up -d --build
    ```
3.  Відкрийте документацію API у браузері:
    👉 **[http://localhost:5099](http://localhost:5099)**

---

### 👨‍💻 Спосіб 2: Запуск для розробки (Гібридний режим)

Цей спосіб використовується для написання коду: база даних працює в Docker, а API запускається локально в IDE для швидкого дебагу.

1.  Запустіть тільки базу даних:
    ```bash
    docker-compose up -d postgres
    ```
2.  Відкрийте файл `CATS.sln` у **Rider**.
3.  Переконайтеся, що `cats-api` в Docker зупинений (щоб звільнити порти).
4.  Запустіть проєкт **Api** (кнопка Run/Debug).
5.  API буде доступне за адресою:
    👉 **[http://localhost:5208](http://localhost:5208)**

---

## 📂 Структура проєкту

Репозиторій розділений на дві основні директорії: **`src`** (вихідний код) та **`tests`** (тести).

### 🏗️ `src` (Clean Architecture)
Основний код застосунку, побудований за принципами чистої архітектури:

* **`Domain`** - Ядро системи. Містить сутності (Entities) та бізнес-логіку, незалежну від зовнішнього світу.
* **`Application`** - Шар сценаріїв використання (Use Cases). Відповідає за обробку команд, запитів (CQRS) та валідацію.
* **`Infrastructure`** - Реалізація взаємодії з інфраструктурою: база даних (EF Core), файлова система, зовнішні сервіси.
* **`Api`** - Точка входу (REST API), конфігурація DI контейнера, middleware та Swagger.

### 🧪 `tests` (Quality Assurance)
Проєкти для забезпечення якості та надійності коду:

* **`Api.Tests.Integration`** - Інтеграційні тести для перевірки повного циклу роботи API endpoint-ів з реальною базою даних.
* **`Tests.Common`** - Базова інфраструктура тестів: налаштування `WebApplicationFactory`, імітація авторизації та допоміжні утиліти.
* **`Tests.Data`** - Генератори тестових даних (Object Mothers/Builders) для створення консистентних сутностей у тестах.

